<Window x:Class="Japanese.MatchGame"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Japanese"
        mc:Ignorable="d"
        Title="Match Game" Height="450" Width="800">

    <Window.DataContext>
        <local:MatchGameViewModel x:Name="ViewModel"/>
    </Window.DataContext>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <ItemsControl Grid.Column="0" ItemsSource="{Binding WordPairs}">
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Button Content="{Binding Japanese}" Margin="5" Width="150" Height="50"/>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>

        <ItemsControl Grid.Column="1" ItemsSource="{Binding WordPairs}">
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Button Content="{Binding Ukrainian}" Margin="5" Width="150" Height="50"/>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>

        <Button Content="Back" Grid.Row="1" Grid.Column="0" Margin="10" Click="BackButton_Click"/>
    </Grid>
</Window>
